# Development + test dependencies — Captura v1.0.0
# Includes all runtime deps via -r requirements.txt.
# Install with:
#   pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==9.0.2
pytest-mock==3.15.1
pytest-cov==7.1.0

# Linting / formatting (zero-config via pyproject.toml)
ruff==0.4.4

# Packaging (only needed when building a distributable .app)
# pyinstaller==6.6.0
